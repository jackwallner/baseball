name: Keepalive

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - run: echo "Keepalive — maintaining workflow scheduler priority."
